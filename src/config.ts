// Feature toggles and lightweight config for game runtime behavior.
// To force a fresh game for today, set RESET_TODAY to true or add today’s ISO date
// (YYYY-MM-DD) to RESET_DATES. Example: ['2025-01-01']
export const RESET_TODAY = true;
export const RESET_DATES: string[] = [];
